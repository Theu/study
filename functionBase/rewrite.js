// function checkAge(age) {
//   if (age > 18) {
//     return true;
//   } else {
//     return confirm('Do you have your parents permission to access this page?');
//   }
// }

// let checkAge = (age) => (age > 18) ? true : confirm('Do you have your parents permission to access this page KK?');

let checkAge = (age) => (age > 18) || confirm('Did parents dd you?');

